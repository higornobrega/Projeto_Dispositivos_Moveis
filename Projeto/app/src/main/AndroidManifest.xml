<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projeto">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Item.ListaDosComValor0"></activity>
        <activity android:name=".Item.CadastrarItem" />
        <activity android:name=".Item.ListarItem" />
        <activity android:name=".Lista.MainActivity" />
        <activity
            android:name=".Lista.CadastroLista"
            android:screenOrientation="portrait" />
        <activity
            android:name=".telas_splash"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>